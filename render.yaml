services:
  - type: web
    name: sigma-finance
    env: python
    rootDir: sigma_finance
    envVars:
      - key: FLASK_APP
        value: sigma_finance.app
    buildCommand: pip install -r requirements.txt
    releaseCommand: |
      echo "----> running Postgres migrations…" &&
      flask db upgrade
    startCommand: gunicorn sigma_finance.app:app
